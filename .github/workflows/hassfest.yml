name: Hassfest

on:
  push:
    branches: [main]
  pull_request:
  release:
    types: [published]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Hassfest
        uses: home-assistant/actions/hassfest@master
